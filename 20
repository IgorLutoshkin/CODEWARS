/* 
В крокетном клубе Western Suburbs есть две категории членства: 
старшая и открытая. Они хотели бы получить вашу помощь с формой 
заявки, в которой будут указаны потенциальные члены, в какую категорию они будут отнесены.

Чтобы быть участником старшего возраста, участнику должно быть 
не менее 55 лет и у него гандикап больше 7. В этом крокетном клубе
 гандикапы варьируются от -2 до +26; чем лучше игрок, тем ниже гандикап.

Ввод
Входные данные будут состоять из списка пар. Каждая пара содержит
 информацию об одном потенциальном участнике. Информация состоит из целого 
 числа для возраста человека и целого числа для его инвалидности.

Результат
Выходные данные будут состоять из списка строковых значений
 (в Haskell и C: Open или Senior), указывающих, следует ли поместить
  соответствующего участника в категорию "старший" или "открытый".

Пример
input =  [[18, 20], [45, 2], [61, 12], [37, 6], [21, 21], [78, 9]]
output = ["Open", "Open", "Senior", "Open", "Open", "Senior"]
*/

function openOrSenior(data) {
  let out = [];

  for (let i = 0; i < data.length; i++) {
    //console.log(data[i] [1]);
    if (data[i][0] >= 55 && data[i][1] > 7) {
      out.push("Senior");
    } else {
      out.push("Open");
    }
  }
  
  return out;
}

console.log(
  openOrSenior([
    [18, 20],
    [45, 2],
    [61, 12],
    [37, 6],
    [21, 21],
    [78, 9],
  ])
);

/*
const openOrSenior = data => 
  data.map(([a, b]) => 
    a >= 55 && b > 7 ? 'Senior' : 'Open'
  )

console.log(openOrSenior([[18, 20],[45, 2],[61, 12],[37, 6],[21, 21],[78, 9]]))
*/
