/*
Фильтровать номер
О, нет! Номер был перепутан с текстом. Ваша цель - извлечь номер из текста, можете ли вы вернуть номер в исходное состояние?

Задача
Ваша задача - вернуть число из строки.

Подробные сведения
Вам будет выдана строка, состоящая из цифр и букв, которые перепутались, вы должны вернуть все числа в этой строке в том порядке, в котором они встречаются.
*/

var filterString = function (value) {
  const arr = value.split(""); // arr
  const filArr = arr.filter((el) => Number(el) || el === '0');
  return Number(filArr.join(''))
};
