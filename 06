
/*Build a function that returns an array of integers from n to 1 where n>0.*/
const reverseSeq = n => {
  const myArray = [];
  let i = n;
  while (i>0) {
  myArray.push(i);
  i--;
  }
  return myArray; 
}
